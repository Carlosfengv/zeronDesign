apiVersion: extensions.agents.x-k8s.io/v1beta1
kind: SandboxWarmPool
metadata:
  name: anydesign-astro-website-pool
  namespace: anydesign-sandboxes
spec:
  replicas: 1
  sandboxTemplateRef:
    name: anydesign-astro-website
