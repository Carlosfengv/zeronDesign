apiVersion: extensions.agents.x-k8s.io/v1beta1
kind: SandboxWarmPool
metadata:
  name: anydesign-fumadocs-docs-pool
  namespace: anydesign-sandboxes
spec:
  replicas: 1
  sandboxTemplateRef:
    name: anydesign-fumadocs-docs
