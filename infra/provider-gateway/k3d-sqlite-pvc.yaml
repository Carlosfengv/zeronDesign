apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: provider-gateway-sqlite
  namespace: provider-system
  labels:
    app.kubernetes.io/name: provider-gateway
    app.kubernetes.io/component: state
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
